[init]
  defaultBranch = main

[color]
  ui = true

[color "status"]
  added = green
  changed = yellow
  untracked = red

[core]
	excludesfile = ~/.config/git/ignore

[rerere]
  enabled = true

[commit]
  verbose = true

[push]
  default = current

[pull]
	rebase = true

[user]
	name = {{ .name }}
	email = {{ .email }}

[github]
  user = jussapaavo

{{ if .is_codespace -}}
[includeIf "gitdir:**/workspaces/*/"]
	path = ~/.config/git/sok
{{ end -}}

{{ if (eq .chezmoi.os "darwin") -}}
[includeIf "gitdir:**/SOK/*/"]
	path = ~/.config/git/sok

[credential]
  helper = osxkeychain
{{ end -}}