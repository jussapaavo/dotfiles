# Use $XDG_CONFIG_HOME instead of root
.gitconfig
.gitignore
.gitignore_global
.gitcookies

# Use $ZDOTDIR instead of root
.zshrc
.zprofile
.zlogin
.zlogout

# Use $XDG_CACHE_HOME instead of root
.zsh_history
.zcompdump*
.bash_history
